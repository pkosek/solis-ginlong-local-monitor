# =============================================================================
# Solar Monitor - Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# At minimum you MUST set INVERTER_IP and LOGGER_SERIAL.
# Everything else has sensible defaults.
# =============================================================================

# --- Inverter connection (REQUIRED) ------------------------------------------

# IP address of the inverter / data-logger stick on your local network
INVERTER_IP=192.168.1.100

# Serial number of the SolarMAN data-logger stick (printed on the stick)
LOGGER_SERIAL=1234567890

# --- Inverter connection (optional) ------------------------------------------

# Modbus TCP port (SolarMAN V5 protocol). Almost always 8899.
# MODBUS_PORT=8899

# Modbus slave ID. Usually 1.
# SLAVE_ID=1

# --- Collection --------------------------------------------------------------

# How often to poll the inverter, in seconds. Default: 300 (5 minutes).
# POLL_INTERVAL=300

# --- Dashboard ----------------------------------------------------------------

# Port the web dashboard listens on. Default: 5000.
# WEB_PORT=5000

# --- Storage ------------------------------------------------------------------

# SQLite database filename (stored in the /data volume inside Docker).
# DB_FILENAME=solar_data.db

# --- System -------------------------------------------------------------------

# Timezone for log timestamps and daily rollover.
TZ=Europe/London
